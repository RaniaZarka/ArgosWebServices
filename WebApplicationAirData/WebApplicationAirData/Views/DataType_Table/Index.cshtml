@model IEnumerable<WebApplicationAirData.Models.DataType_Table>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DataTypeNavn)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DataTypeNavn)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DataTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DataTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DataTypeId })
        </td>
    </tr>
}

</table>
